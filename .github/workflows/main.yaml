name: CI Mentoria

All Bookmarks

Summary

on:
 workflow_dispatch:

#gatithos

 jobs:

  tests:


   #Iconfig de sol (windows/Linux/mac, chrome/firefox/edge, etc)
   runs-on: ubuntu-latest

   #Ivariavels) (variáveis de anbiente, chaves de acesso a plataformas, etc.)
   env:
   API BASE URL: http://localhost:3000
   PORT: 4808

   #lacoes ou passos
   steps!

    #[projetos] (aplicacoes, projeta de teste, etc.)

   -name: Checkout do Repositorio
    uses: actions/checkoutgv4

   #[deps do projetol Inade/npm, Java/maven.gradle, python/pip,...)

   -name: Instalando as dependencias
    run: nom install

   -name: Iniciar o servidor de mocks
    run: прв run mock-server &
 
   -name: Iniciar servidor de aplicacao
    run: nom run server &

   -name: Executar os testes
run